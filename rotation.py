import numpy as np
import Embryo
import Boundary

class Rotate:
    angle = 0
    raw_image =  Embryo()
    rotated_image = Embryo()
    boundary = Boundary()

    def __init__(self,raw_image)
    def set_boundary
    
    def detect_angle(self,boundary()):

        self.angle = 


    def rotate_embryo(self,boundary.raw_image):

        rotated_image =

    def get_rotated:
        return rotated_image



        r = Rotate(Embryo())
        abc = r.rotated_image
        abc = r.get_rotated
